{
  "name": "repo-doctor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "server": "node server/index.mjs",
    "dev:all": "npm run server & npm run dev",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "desktop:preview": "npm run build && electron .",
    "desktop:package": "npm run build && electron-builder --mac dmg"
  },
  "devDependencies": { "@types/node": "latest", "electron": "latest", "electron-builder": "latest", "typescript": "latest", "vite": "latest" }
}
