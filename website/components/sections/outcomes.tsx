const stats = [["1", "Treatment applied at a time"], ["100%", "Changes require your approval"], ["0", "Files touched without a diff shown first"], ["Isolated", "Every repair runs in a working copy, never the original"]] as const;
export function Outcomes() { return <section className="border-y border-accent/15 bg-background-elevated"><div className="mx-auto grid max-w-7xl divide-y divide-accent/15 px-5 sm:grid-cols-2 sm:divide-x sm:divide-y-0 lg:grid-cols-4 lg:px-8">{stats.map(([number, label]) => <div key={number} className="py-9 sm:px-7 sm:first:pl-0"><p className="font-mono text-5xl font-bold tracking-[-.08em] text-accent">{number}</p><p className="mt-3 max-w-44 text-sm leading-6 text-text-muted">{label}</p></div>)}</div></section>; }
