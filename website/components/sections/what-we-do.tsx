const pillars = [["01", "Diagnose", "Finds what’s actually wrong, with evidence.", ["Broken imports", "Dead files", "Hardcoded secrets", "Weak tests"]], ["02", "Explain", "Shows why it matters, in plain language.", ["Severity", "Confidence", "Affected files", "Root cause"]], ["03", "Repair", "Fixes one issue at a time, safely.", ["Isolated working copy", "Readable diff", "Your approval required", "Full verification"]]] as const;
export function WhatWeDo() { return <section id="diagnoses" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><p className="font-mono text-xs uppercase tracking-[.16em] text-accent">Clinical approach</p><h2 className="mt-4 max-w-2xl text-4xl font-bold tracking-[-.04em] sm:text-5xl">Not just another AI code reviewer.</h2><div className="mt-12 grid gap-5 md:grid-cols-3">{pillars.map(([number, title, description, bullets]) => <article key={title} className="rounded-lg border border-accent/15 bg-background-elevated p-7 shadow-lg shadow-black/10"><span className="font-mono text-sm text-accent">{number}</span><h3 className="mt-8 text-2xl font-bold">{title}</h3><p className="mt-3 leading-7 text-text-muted">{description}</p><ul className="mt-7 space-y-2 border-t border-accent/15 pt-5">{bullets.map((bullet) => <li key={bullet} className="font-mono text-xs"><span className="mr-2 text-accent">↗</span>{bullet}</li>)}</ul></article>)}</div></section>; }
