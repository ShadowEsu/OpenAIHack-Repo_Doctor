const stack = ["JavaScript", "TypeScript", "Python", "Go", "React", "Next.js", "GitHub", "npm", "pip"];
export function TechMarquee() { const items = [...stack, ...stack]; return <section aria-label="Supported technologies" className="border-y border-accent/15 bg-background-elevated py-5"><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="mb-3 font-mono text-xs uppercase tracking-[.16em] text-text-muted">Works with the stack you already use.</p></div><div className="marquee overflow-hidden"><div className="marquee-track">{items.map((item, index) => <span key={`${item}-${index}`} className="mx-3 inline-flex rounded-full border border-accent/15 px-4 py-2 font-mono text-sm text-text-muted">{item}</span>)}</div></div></section>; }
