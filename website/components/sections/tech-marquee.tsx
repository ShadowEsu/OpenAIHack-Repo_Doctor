import { Reveal } from "@/components/Reveal";
const stack = [{ label: "JavaScript", icon: "javascript" }, { label: "TypeScript", icon: "typescript" }, { label: "Python", icon: "python" }, { label: "React", icon: "react" }, { label: "Next.js", icon: "nextdotjs" }, { label: "GitHub", icon: "github" }, { label: "npm", icon: "npm" }, { label: "pip", icon: "pypi" }];
export function TechMarquee() { const renderStack = (duplicate = false) => <div className="flex shrink-0" aria-hidden={duplicate || undefined}>{stack.map((item) => <span key={`${duplicate ? "duplicate-" : ""}${item.label}`} className="mx-3 inline-flex items-center gap-2 rounded-full border border-accent/15 px-4 py-2 font-mono text-sm text-text-muted"><img className="h-8 w-8" src={`https://cdn.simpleicons.org/${item.icon}/EAFBF8`} alt="" aria-hidden="true" />{item.label}</span>)}</div>; return <section aria-label="Supported technologies" className="border-y border-accent/15 bg-background-elevated py-5"><Reveal><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="mb-3 font-mono text-xs uppercase tracking-[.16em] text-text-muted">Works with the stack you already use.</p></div><div className="marquee overflow-hidden"><div className="marquee-track">{renderStack()}{renderStack(true)}</div></div></Reveal></section>; }
