"use client";

import { motion, useReducedMotion } from "framer-motion";
const steps = [["01", "Intake", "Provide a GitHub URL or ZIP upload. We map the repo structure, detect languages and frameworks."], ["02", "Examination", "Deterministic scanners check imports, secrets, tests, and structure first — before any AI runs."], ["03", "Diagnosis", "AI explains findings, estimates confidence and risk, and prioritizes what matters most."], ["04", "Treatment", "You review the proposed fix and diff, approve it, and Repo Doctor verifies tests still pass."]] as const;
export function HowItWorks() { const reduceMotion = useReducedMotion(); return <section id="how-it-works" className="mx-auto grid max-w-7xl gap-12 px-5 py-24 lg:grid-cols-[.7fr_1.3fr] lg:px-8"><motion.div className="lg:sticky lg:top-28 lg:self-start" initial={reduceMotion ? false : { opacity: 0, y: 20 }} whileInView={reduceMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: false, margin: "-50px" }} transition={{ duration: .5 }}><p className="font-mono text-xs uppercase tracking-[.16em] text-accent">The process</p><h2 className="mt-4 text-4xl font-bold tracking-[-.04em] sm:text-5xl">A measured path to a healthier repo.</h2></motion.div><div className="divide-y divide-accent/15 border-y border-accent/15">{steps.map(([number, title, copy]) => <motion.article key={number} className="grid gap-4 py-9 sm:grid-cols-[5rem_1fr]" initial={reduceMotion ? false : { opacity: 0, y: 20 }} whileInView={reduceMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: false, margin: "-50px" }} transition={{ duration: .45, ease: "easeOut" }}><span className="font-mono text-xl text-accent">{number}</span><div><h3 className="text-2xl font-bold">{title}</h3><p className="mt-3 max-w-xl leading-7 text-text-muted">{copy}</p></div></motion.article>)}</div></section>; }
