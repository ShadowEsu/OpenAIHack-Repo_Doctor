"use client";

import { CountUp } from "@/components/CountUp";
import { motion, useReducedMotion } from "framer-motion";

export function BeforeAfter() { const reduceMotion = useReducedMotion(); const variant = (delay: number) => ({ initial: reduceMotion ? false : { opacity: 0, y: 24 }, whileInView: reduceMotion ? {} : { opacity: 1, y: 0 }, viewport: { once: false, margin: "-50px" }, transition: { duration: .65, delay } }); return <section id="verification" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><motion.div {...variant(0)}><p className="font-mono text-xs uppercase tracking-[.16em] text-accent">Verification record</p><h2 className="mt-4 text-4xl font-bold tracking-[-.04em] sm:text-5xl">Show the difference.</h2></motion.div><div className="mt-12 grid max-w-4xl gap-5 sm:grid-cols-2"><motion.div {...variant(.08)} className="rounded-lg border border-status-warning/30 bg-background-elevated p-8"><p className="font-mono text-xs text-text-muted">BEFORE</p><p className="mt-3 text-7xl font-bold tracking-[-.08em] text-status-warning"><CountUp value={58} /></p><p className="mt-2 text-text-muted">Health Score</p></motion.div><motion.div {...variant(.16)} className="rounded-lg border border-status-success/30 bg-background-elevated p-8"><p className="font-mono text-xs text-text-muted">AFTER</p><p className="mt-3 text-7xl font-bold tracking-[-.08em] text-status-success"><CountUp value={82} /></p><p className="mt-2 text-text-muted">Health Score</p></motion.div></div></section>; }
