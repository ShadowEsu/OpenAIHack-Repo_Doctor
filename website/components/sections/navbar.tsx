import Image from "next/image";

const links = [["Overview", "#overview"], ["Diagnoses", "#diagnoses"], ["Repository Map", "#how-it-works"], ["Treatments", "#verification"], ["How it Works", "#how-it-works"]] as const;
export function Navbar() { return <header className="sticky top-0 z-50 border-b border-accent/15 bg-background/95 backdrop-blur"><nav className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8" aria-label="Main navigation"><a href="#overview" className="flex items-center gap-2 text-lg font-bold tracking-tight"><Image src="/logo.png" alt="Repo Doctor Logo" width={32} height={32} className="object-contain" />Repo <span className="text-accent">Doctor</span></a><div className="hidden items-center gap-6 text-sm text-text-muted lg:flex">{links.map(([label, href]) => <a key={label} href={href} className="transition-colors hover:text-accent">{label}</a>)}</div><a href="#health-report" className="rounded-md bg-accent px-4 py-2 text-sm font-semibold text-background transition-colors hover:bg-accent-secondary hover:text-text-primary">Examine a Repository</a></nav></header>; }
